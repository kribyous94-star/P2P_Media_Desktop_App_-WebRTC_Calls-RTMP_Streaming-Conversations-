// Phase 2 — Auth : ce composant sera complété
export default function LoginPage() {
  return (
    <div style={{ display: "flex", alignItems: "center", justifyContent: "center", height: "100%" }}>
      <h1>Login — Phase 2</h1>
    </div>
  );
}
